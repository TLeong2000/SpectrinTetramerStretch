#!/bin/sh
#PBS -l select=1:system=polaris
#PBS -l place=pack
#PBS -l walltime=1:00:00
#PBS -l filesystems=home:eagle:grand
#PBS -q debug
#PBS -A ModelingCoronaVirus
#PBS -N Raw_WLC_Relaxation
#PBS -o Out_relaxation/nohup.out
#PBS -e Out_relaxation/nohup.err

cd $PBS_O_WORKDIR

> Out_relaxation/nohup.out
> Out_relaxation/nohup.err

echo "PWD = $PWD"

# export MPICH_GPU_SUPPORT_ENABLED=1

#NNODES=`wc -l < $PBS_NODEFILE`
NNODES=1

# per-node settings
# NRANKS=4
# NDEPTH=8
NRANKS=1
NDEPTH=1
NTHREADS=1

NTOTRANKS=$(( NNODES * NRANKS ))

#. ./setup_lammps_gnu.sh

# EXE=/home/knight/bin/lmp_polaris_gnu_kokkos
# EXE_ARG="-in in.reaxc.hns -k on g ${NGPUS} -sf kk -pk kokkos neigh half neigh/qeq full newton on "

# OMP settings mostly to quiet Kokkos messages


EXE="./lmp_polaris_gnu"
# EXE="../IdealizedChainStretch/lmp_polaris_gnu"
echo "EXE directory = $EXE"

EXE_ARG="-in RawWLC11Relaxation.lammps -nonbuf"
# EXE_ARG="-in RawWLC21Relaxation.lammps RawWLC41Relaxation.lammps"

# MPI_ARG=" -n ${NTOTRANKS} --ppn ${NRANKS} --depth=${NDEPTH} --cpu-bind depth "
MPI_ARG=" -n ${NTOTRANKS} --ppn ${NRANKS} --depth=${NDEPTH} --cpu-bind none "
# --cpu-bind is apparently unrecognized by mpirun here
# MPI_ARG=" -n ${NTOTRANKS} --ppn ${NRANKS} --depth=${NDEPTH} "
OMP_ARG=" -env OMP_NUM_THREADS=${NTHREADS} -env OMP_PROC_BIND=spread -env OMP_PLACES=cores "

COMMAND="mpiexec ${MPI_ARG} ${OMP_ARG} $EXE ${EXE_ARG}"
# COMMAND="$EXE ${EXE_ARG}"
echo "COMMAND= ${COMMAND}"
${COMMAND}
